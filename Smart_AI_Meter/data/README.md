This data folder has 3 sub folders

The raw folder will have the CSV file which comes from the streamlit frontend.

The processed folder will have the cleaned data on which the model will be trained. 

The Prediction will have the predicted values of the next week with the next week's temperature in CSV file.